[package]
name = "decls-gen"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib", "dylib"]

[package.metadata.rust-analyzer]
# This package uses #[feature(rustc_private)]
rustc_private = true

[dependencies]
